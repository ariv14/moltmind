{
  "name": "moltmind",
  "version": "0.3.0",
  "description": "Agent Memory MCP Server — persistent semantic memory and session continuity for AI agents",
  "type": "module",
  "bin": {
    "moltmind": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "test": "tsx --test tests/*.test.ts",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "mcp",
    "memory",
    "ai",
    "agent",
    "semantic-search",
    "embeddings",
    "sqlite"
  ],
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ariv14/moltmind.git"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "better-sqlite3": "^11.0.0",
    "@xenova/transformers": "^2.17.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0"
  }
}
